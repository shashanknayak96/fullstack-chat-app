kind: PersistentVolume
apiVersion: v1
metadata:
  name: mongodb-pv
  labels:
    app: mongodb-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  hostPath:
    path: /mnt/data

